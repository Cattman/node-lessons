//run this in terminal using 'node Lesson2/hello' or 'node Lesson2/hello.js'
'use strict';
console.log('hello, node!');

/*
    load variable into REPL 
    open REPL using keyword 'node'
    call module '.load' followed by the file path
    now you can use the variables in REPL
    after using it in REPL you can save your work with .save *filename*
*/
let messages = [
    "A first message",
    "Another message to send",
    "Last message to send"
];